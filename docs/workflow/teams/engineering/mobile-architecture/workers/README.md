# Mobile Architecture Workers

This folder is for durable worker-specific handoffs, not per-phase dispatch.

- `worker-a/`
  - durable cleanroom handoff prompt
- `worker-b/`
  - durable cleanroom handoff prompt
- `worker-c/`
  - durable cleanroom handoff prompt
