# Mobile Records

Put mobile completion records, decision logs, and milestone checkpoints here.

Examples:
- phase completion note
- rollback record
- shipped milestone record
- historical checkpoint tied to a governed doc lifecycle

Rules:
- records describe what happened
- they do not replace current product or architecture truth
- if a record closes a doc lifecycle, make sure `docs/initiatives/TIMELINE.md` is updated too
