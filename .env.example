# Copy this file to `.env` before running docker compose.
# Admin console password used by frontend `/auth` login.
ADMIN_CONSOLE_PASSWORD=change-this-admin-password

# Salt used for signing admin session cookie token.
ADMIN_CONSOLE_SESSION_SALT=change-this-cookie-salt
